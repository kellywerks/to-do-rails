<% content_for(:title, "List | To Do") %>

<h1><%= @list.name %></h1>

<h4>Here are the tasks for this list:</h4>

<ul id='list_tasks'>
  <% if @list.tasks.any? %>
    <% @list.tasks.each do |task| %>
      <li>
        <%= task.description %> |
        <%= link_to "Edit", edit_list_task_path(@list, task), id: "edit_task_btn",:class => "btn btn-default", remote: true %> |
        <%= link_to "Delete", list_task_path(@list, task),
                                 :data => {:confirm => "Are you sure?",
                                 :method => "delete"},
                                 :class => "btn btn-danger" %>
      </li>
    <% end %>
<% else %>
  <p>None yet!</p>
<% end %>
</ul>

<div id="modal" class="modal" style="display: none; ">
  <p>
    Hey hey hey check me out, I'm a task-adding modal!

  </p>
</div>

<%=  link_to_modal "Modal", "#modal", :class=>"button" %>
<%=  link_to_modal "New list", new_list_path, :class => "btn btn-default" %>

<p><%= link_to_modal "Add a task", new_list_task_path(@list), id: "new_task_link", class: "btn btn-default", remote: true %></p>
<p><%= link_to "Delete List", list_path(@list),
                        :data => {:confirm => "Are you sure?",
                        :method => "delete"},
                        :class => "btn btn-danger" %></p>

<p><%= link_to "Return to lists", lists_path, :class => "btn btn-default" %></p>
